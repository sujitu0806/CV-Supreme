# Metadata exports

Save exported shot metadata JSON files here. Use the **Export metadata** button in the app to download the current session’s shots, then save the file into this folder for later analysis or aggregation.

Filename format: `shots_YYYY-MM-DD_HH-mm-ss.json`
