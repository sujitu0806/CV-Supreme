# Training mode metadata exports

Session JSON files from training mode are saved here automatically when you stop a session. Format matches comp_mode metadata exports: `sessionId`, `startedAt`, `endedAt`, `observations`, and `mediapipe` (ROM, paddle, frameSample every 4 frames).

Filename format: `session_<sessionId>.json`
